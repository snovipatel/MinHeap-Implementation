# MinHeap-Implementation
This project implements a Min Heap in Java using an array.

Features:
- Insert elements into the heap
- Remove the minimum element
- Automatic resizing of the internal array
- HeapSort using the heap

The project also includes a test class that demonstrates different cases such as duplicates, sorted input, reverse sorted input, and edge cases.
